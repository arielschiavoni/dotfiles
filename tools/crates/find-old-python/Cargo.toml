[package]
name        = "find-old-python"
description = "Scan macOS for Python installations below a version threshold"
# Inherited from [workspace.package] in ../../Cargo.toml
version.workspace    = true
edition.workspace    = true
license.workspace    = true
repository.workspace = true

[dependencies]
# `workspace = true` pulls the version pinned in the workspace manifest.
anyhow = { workspace = true }
clap   = { workspace = true }
ignore = { workspace = true }
